# Control-plane Lambda dependencies. Deliberately pipecat-free: the API host
# imports only voice_kit's control-plane surface (see runtime/pyproject.toml).
#
# Backend CI reads the ruff pin straight out of this file (`grep '^ruff=='`),
# and this is the only requirements file that installs pytest — keep both.

fastapi==0.141.1
# The vendored voice_kit package (control-plane surface only — its
# pyproject.toml carries no pipecat). Single source of truth for CI and local
# dev; api/Dockerfile.api installs the same deps in its own layered form.
./runtime
# The image's ASGI server: api/Dockerfile.api runs `uvicorn api.main:app` under
# the Lambda Web Adapter, and `npm run dev:api` runs the same command locally.
uvicorn[standard]==0.52.4
# ASGI -> Lambda adapter. Not what production invokes (LWA proxies to the ASGI
# app directly) — kept as the zip-packaging fallback, and covered by api/tests.
mangum==0.21.0

# Tooling (CI + local): the lint job installs the ruff line, the test job runs
# pytest over both api/tests and runtime/tests, and httpx backs fastapi's
# TestClient. api/Dockerfile.api's dependency layer strips these three lines by
# name — add a tooling pin here and add it there too.
ruff==0.16.3
pytest==9.1.1
httpx==0.28.1
